# Real CloudWatch log exports — not committed (see README)
data/raw/

# SQLite database — recreate by running the detector
db/

# Environment variables — never commit secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Build artifacts
design.html
build_html.py

# OS
.DS_Store
Thumbs.db
